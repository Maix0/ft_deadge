input {
  gelf {
    port => 12201
  }
}

filter {
  mutate {
    rename => { "[full_message]" => "message" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "docker-%{[container_name]}-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}
